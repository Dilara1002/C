	for(i=0;i<50;i++)
	{
		
	}#include <stdio.h>
#include <stdlib.h>
/*#include <time.h>
/* run this program using the console pauser or add your own getch, system("pause") or input loop */
/*void erase(int D[],int on,int n)
{
	int i;
	if(on>=n+1 || on<0)
	{
		printf("Mistake!\n");
	}
	else
	{
		for(i=on-1;i<n-1;i++)
		{
			D[i]=D[i+1];
		}
		printf("Array that after selected number is deleted:\n");
		for(i=0;i<n-1;i++)
		{
			printf("%d\n",D[i]);
		}
	}
	
}
int main(int argc, char *argv[]) {
	int n,i;
	int ordinalno;
	printf("Enter a number for array:");
	scanf("%d",&n);
	int d[n];
	srand(time(0));
	for(i=0;i<n;i++)
	{
		d[i]=rand()%100;
		printf(	"%d\n",d[i]);
	}
	printf("What is the ordinal no of the number to be deleted:");
	scanf("%d",&ordinalno);
	erase(d,ordinalno,n);
	return 0;
}*/


