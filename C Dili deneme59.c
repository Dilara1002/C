#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

/*int main(void) {
	char s;
	int count[26]={0};
	int dilara,character,i;
	character=0;
	printf("Enter a sentence:");
	while((s=getchar())!='\n')
	{
		dilara=s-'a';
		if(dilara>=0 && dilara<26)
		count[dilara]++;
		character++;
	}
	printf("\nEntry sentence contains up to %d characters:\n",character);
	for(i=0;i<26;i++)
	{
		printf("There is %d times %c\n",count[i],'a'+i);
	}
	
	return 0;
}*/



int main(void) {
	
	
	return 0;
}
